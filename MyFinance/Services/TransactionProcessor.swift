import SwiftData
import Foundation

// TransactionProcessor is being rebuilt for v3 models.
// Pocket saldo mutations are now handled inline at the call site.
struct TransactionProcessor {
    // Placeholder — will be reimplemented in Phase 2
}
